# NS_Solver Example Runtime Configuration
# Recommended example using the current unified config keys.

# Grid parameters
nx = 320
ny = 128

# Domain dimensions
lx = 25.0
ly = 10.0

# Physics parameters
re = 100.0

# Boundary conditions
# Face types: inflow | farfield | outflow | wall | periodic
bc_left = inflow
bc_right = outflow
bc_bottom = farfield
bc_top = farfield

# Inflow/farfield velocity components
inflow_u = 1.0
inflow_v = 0.0
initial_v_perturbation_pct = 2.0
inflow_w = 0.0

# Wall behavior (used where face type is 'wall')
# wall_slip_mode: no-slip | free-slip
wall_slip_mode = no-slip
wall_penetration = false
wall_normal_velocity = 0.0

# Outflow behavior (used where face type is 'outflow')
# outflow_mode: convective | zero-gradient
outflow_mode = convective
outflow_speed = 1.0

# Time integration
t_end = 100.0
cfl = 0.5

# I/O parameters
save_dt = 0.02
outdir = output

# Grid generation used by both main.py and pre_generate_grid.py
# uniform_grid = true  -> uniform mesh
# uniform_grid = false -> nonuniform mesh
uniform_grid = false
# Nonuniform grids use a uniform central core with tanh-stretched outer regions.
grid_beta_x = 2.0
grid_beta_y = 2.0
grid_uniform_x_start = 6.0
grid_uniform_x_end = 10.0
grid_uniform_y_start = 3.5
grid_uniform_y_end = 6.5

# Features (true/false)
cylinder = true

# Cylinder
cylinder_center_x = 8.0
cylinder_center_y = 5.0
cylinder_radius = 0.5
re_is_cylinder_based = true
cylinder_rotation_mode = stationary
cylinder_rotation_amplitude = 0.0
cylinder_rotation_frequency = 0.0
cylinder_rotation_phase_deg = 0.0
verbose = true

# Optional aerodynamic post-processing normalization used by
# analyze_aerodynamics.py when needed.
# aero_length_scale = 1.0
# aero_use_cylinder_diameter = true
